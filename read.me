nest url in a different way to associate only user's own notes 

router.get('/user/:userId/poem', verifyToken, poemController.getPoemsByUserId);

SSL refers to https. Be careful when writing endpoints http/https
